# Netlify deployment configuration for StrikersFeed (Next.js App Router).
# The official Next.js runtime plugin handles SSR, image optimisation and routing.

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
